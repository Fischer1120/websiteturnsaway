/*
 * Public content is server-rendered by Astro fallback pages and Pages Functions.
 * This inert compatibility asset is intentionally kept for older cached HTML;
 * it performs no fetches and never exposes R2 metadata.
 */
